[tool.poetry]
name = "tap-jira"
version = "0.0.1"
description = "`tap-jira` is a Singer tap for tap-jira, built with the Meltano Singer SDK."
readme = "README.md"
authors = ["Ethan Stein"]
keywords = [
    "ELT",
    "tap-jira",
]
license = "Apache-2.0"

[tool.poetry.dependencies]
python = ">=3.9"
singer-sdk = "~=0.43.1"
fs-s3fs = { version = "^1.1.1", optional = true }
requests = "~=2.32.2"
boto3 = "1.35.85"
inflection = "^0.5.1"
record-cleanser = {path = "../../helpers/record-cleanser/dist/record_cleanser-0.0.4-py3-none-any.whl"}

[tool.poetry.group.dev.dependencies]
pytest = ">=8"
singer-sdk = { version="*", extras = ["testing"] }

[tool.poetry.extras]
s3 = ["fs-s3fs"]

[tool.mypy]
python_version = "3.9"
warn_unused_configs = true

[tool.ruff]
target-version = "py39"

[tool.ruff.lint]
select = ["ALL"]

[tool.ruff.lint.flake8-annotations]
allow-star-arg-any = true

[tool.ruff.lint.isort]
known-first-party = ["tap_jira"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[build-system]
requires = ["poetry-core>=1.0.8"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
# CLI declaration
tap-jira = 'tap_jira.tap:TapJira.cli'
